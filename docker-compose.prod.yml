version: "3.9"

services:

  portfolio:

    container_name: portfolio-prod

    build:
      context: .
      dockerfile: Dockerfile.prod

    ports:
      - "3000:3000"

    env_file:
      - .env.production

    environment:
      NODE_ENV: production

    restart: unless-stopped